
const express = require("express");
const path = require("path");

class Server {
    constructor(port, path) {
        this.port = port || 8000;
        this.path = path || path.join(__dirname, "index.html");
    }

    start() {

    }
}

module.exports = Server;